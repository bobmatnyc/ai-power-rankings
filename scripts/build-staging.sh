#!/bin/bash
# Temporary build script for staging to isolate issues

echo "Starting staging build..."
echo "Skipping static rankings generation for debugging"
echo "Running Next.js build only..."

next build